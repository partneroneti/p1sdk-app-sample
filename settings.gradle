pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flatDir{ dirs 'libs/' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        flatDir{ dirs 'libs/' }
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "ProjetoExemplo"
include ':app'
include ':photoface'
